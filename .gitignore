# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Backups / restores
backups/
*.bak

# Runtime state / output
user_settings/
logs/
output/
tmp/
.deps_installed
.venv/

# Test/output artifacts (large binaries)
shootout_out/
compression_testing_videos/
test_pipeline/out/
test_pipeline/*.log
test_pipeline/*.err
test_pipeline/corpus/
test_pipeline/results_*.json

# Vendored/fetched binary tools (Windows .exe + Linux/macOS binaries + shims)
tools/*.exe
tools/ffmpeg
tools/ffprobe
tools/HandBrakeCLI

# Release bundle build output (per-OS zips from build_bundle.py)
dist/

# Dev-only test suites — kept locally, not published to the repo.
tests/
test_pipeline/

# Local tooling / state
.claude/
CLAUDE.md
_bmad/
_bmad-output/

# Private dev/testing material (never ship in the public repo)
Comparison_testing/
TESTING_DATA/
test_pipeline/*.json
