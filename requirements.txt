# BitCrusher dependencies
# Install with: pip install -r requirements.txt
#
# Also required (not pip-installable):
#   - ffmpeg  (must be on PATH) — https://ffmpeg.org/download.html
#   - Python 3.10+

# Core numeric / ML
numpy>=1.24
scipy>=1.10
scikit-learn>=1.2

# Image processing (used by ml_heuristics for frame sampling)
Pillow>=9.0
opencv-python>=4.7

# System / process utilities
psutil>=5.9
requests>=2.28

# GUI extras
tkinterdnd2>=0.3        # drag-and-drop support
pystray>=0.19           # system tray icon
plyer>=2.1              # cross-platform desktop notifications
win10toast>=0.9 ; sys_platform == "win32"   # Windows toast notifications (Windows only)

# File watching & media
watchdog>=3.0           # folder watch / auto-queue
yt-dlp>=2023.1.6        # URL download support

# GUI (tkinter ships with Python — no pip install needed)
# If your Python was built without Tk, install "python3-tk" via your OS package manager.
